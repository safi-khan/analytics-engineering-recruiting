version: 2

seeds:
  - name: merchants
    description: '{{ doc("stg__merchants") }}'

  - name: orders
    description: '{{ doc("stg__orders") }}'

  - name: returns
    description: '{{ doc("stg__returns") }}'
